# Ejemplo de fichero .env para desarrollo local
# Copia este archivo a `.env` y rellena con tus valores locales.

# URI de conexión a MongoDB Atlas (no dejes credenciales en el repo)
MONGO_URI="mongodb+srv://<usuario>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority"

# Clave secreta de Flask (usa una cadena larga en producción)
SECRET_KEY="cambia_esto_por_una_clave_larga_y_segura"

# Puerto opcional (por defecto en el proyecto se usa 5001)
PORT=5001

# Ejemplo de cómo exportar variables desde .env (zsh / bash):
# export $(grep -v '^#' .env | xargs)
